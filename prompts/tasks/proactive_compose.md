# 主动消息生成任务

根据唯一选中的候选，写一条自然、克制、可直接发送的简体中文私聊消息。

- 只使用候选及 source_refs 支持的外部事实。长期记忆只能帮助调整语气和选择自然切入点，不得被写成候选事实，也不要突兀暴露与当前内容无关的私人记忆。
- 不声称执行过未提供的查询，不催促用户回复。
- 有 URL 时可自然附上原 URL；不得生成或改写 URL。
- 文本不超过 500 个字符，不使用 Markdown 表格。
- 只输出 JSON：`{"text":"最终消息"}`。
